# General Functions

__bash_emit() {
    local output_file="$1"
    local format="$2"
    shift
    shift

    if [[ -n "$output_file" ]]; then
        printf "$format" "$@" >> "$output_file"
    else
        printf "$format" "$@"
    fi
}

source_bash_d_files() {
    local output_file="${1:-}"
    local output_style="${2:-terminal}"
    local bash_d="${BASH_D:-$HOME/.bash.d}"
    local total_length=45
    local totalfiles=0
    local color_start=""
    local color_end=""

    case "$output_style" in
        fastfetch)
            color_start='$2'
            color_end='$1'
            ;;
        terminal)
            color_start="${FG_GREEN:-}"
            color_end="${TXT_RESET:-}"
            ;;
    esac

    if [[ -d "$bash_d" ]]; then
        __bash_emit "$output_file" "╔═════════════════════════════════════════════╗\n"
        __bash_emit "$output_file" "║                %sSOURCED FILES%s                ║\n" "$color_start" "$color_end"
        __bash_emit "$output_file" "╠═════════════════════════════════════════════╣\n"

        for file in "$bash_d"/*; do
            if [[ -f "$file" && -r "$file" ]]; then
                ((totalfiles++))
                source "$file"

                local filename
                local filename_length
                local padding_length
                local padding=""
                local i

                filename=$(basename "$file")
                filename_length=${#filename}
                padding_length=$(( (total_length - filename_length) / 2 ))

                for ((i=0; i<padding_length; i++)); do
                    padding+=" "
                done

                if (( (total_length - filename_length) % 2 != 0 )); then
                    filename="$filename "
                fi

                __bash_emit "$output_file" "║%s%s%s%s%s║\n" "$padding" "$color_start" "$filename" "$color_end" "$padding"
            fi
        done

        __bash_emit "$output_file" "╚═════════════════════════════════════════════╝"
    else
        echo "Directory $bash_d does not exist."
        return 1
    fi

    SOURCE_BASH_D_TOTALFILES=$totalfiles
}

show_daily_fastfetch() {
    local state_home="${XDG_STATE_HOME:-$HOME/.local/state}"
    local stamp_dir="$state_home/shell-motd"
    local logo_file="${LOGO_FILE:-$stamp_dir/fastfetch-logo.txt}"
    local config_file="${CONFIG_FILE:-$HOME/.config/fastfetch/config_src.jsonc}"
    local generated_config="$stamp_dir/fastfetch-config.jsonc"
    local fastfetch_status
    local newcomp_file="$HOME/.config/fastfetch/newcomp.txt"
    local top_value

    [[ -f "$newcomp_file" ]] || return 1
    [[ -f "$config_file" ]] || return 1
    command -v jq >/dev/null 2>&1 || return 1
    command -v fastfetch >/dev/null 2>&1 || return 1
    mkdir -p "$stamp_dir"

    cat "$newcomp_file" > "$logo_file"
    printf "\n" >> "$logo_file"

    if ! source_bash_d_files "$logo_file" fastfetch; then
        rm -f "$logo_file"
        return 1
    fi

    printf "\n" >> "$logo_file"

    top_value=$(( (13 - SOURCE_BASH_D_TOTALFILES) / 2 ))
    if (( top_value < 0 )); then
        top_value=0
    fi

    if ! jq --argjson newTop "$top_value" --arg logoSource "$logo_file" \
        '.logo.padding.top = $newTop | .logo.source = $logoSource' \
        "$config_file" > "$generated_config"; then
        rm -f "$logo_file" "$generated_config"
        return 1
    fi

    fastfetch --config "$generated_config"
    fastfetch_status=$?
    rm -f "$logo_file" "$generated_config"
    return "$fastfetch_status"
}

bash_daily_motd() {
    local state_home="${XDG_STATE_HOME:-$HOME/.local/state}"
    local stamp_dir="$state_home/shell-motd"
    local stamp_file="$stamp_dir/bash-fastfetch.last"
    local today

    today=$(date +%F)
    mkdir -p "$stamp_dir"

    if [[ -r "$stamp_file" ]] && [[ "$(cat "$stamp_file")" == "$today" ]]; then
        re-source
        return
    fi

    if show_daily_fastfetch; then
        printf '%s\n' "$today" > "$stamp_file"
    else
        re-source
    fi
}

# Re-source bash files
re-source() {
    source_bash_d_files "" terminal
    printf "\n"
}

# Recursively find a directory
cdfind() {
    local search_pattern="$1"
    local base_dir="${2:-.}"
    local found_dir

    if [[ -z "$search_pattern" ]]; then
        echo "Usage: cdfind <search-pattern> [base-directory]"
        return 1
    fi

    case "$base_dir" in
        "~") base_dir="$HOME" ;;
        "~/"*) base_dir="$HOME/${base_dir#~/}" ;;
    esac

    search_pattern="*$search_pattern"
    found_dir=$(find "$base_dir" -type d -path "$search_pattern" -print -quit 2>/dev/null)

    if [[ -n "$found_dir" ]]; then
        cd "$found_dir" || return 1
        echo "Changed directory to: $found_dir"
    else
        echo "Directory matching '$search_pattern' not found in '$base_dir'."
        return 1
    fi
}

# Watch tail of a file
wt() {
    if [ "$#" -ne 3 ]; then
        echo "Usage: wt <seconds> <lines> <file>"
        return 1
    fi

    local seconds=$1
    local lines=$2
    local file=$3

    if ! [[ "$seconds" =~ ^[0-9]+$ ]] || [ "$seconds" -le 0 ]; then
        echo "Error: <seconds> must be a positive integer."
        return 1
    fi

    if ! [[ "$lines" =~ ^[0-9]+$ ]] || [ "$lines" -le 0 ]; then
        echo "Error: <lines> must be a positive integer."
        return 1
    fi

    if [ ! -f "$file" ]; then
        echo "Error: File '$file' does not exist."
        return 1
    fi

    watch -n "$seconds" "tail -n $lines $file"
}

# User for ssh
user() {
    local ip=""

    if [[ "$(uname)" == "Darwin" ]]; then
        ip=$(ipconfig getifaddr en0 2>/dev/null || ipconfig getifaddr en1 2>/dev/null)
    elif command -v hostname >/dev/null 2>&1; then
        ip=$(hostname -I 2>/dev/null | awk '{print $1}')
    fi

    if [[ -n "$ip" ]]; then
        printf '%s@%s\n' "$(whoami)" "$ip"
    else
        whoami
    fi
}

# Print path with newlines
pathd() {
    local path_value="${1:-$PATH}"

    printf '%s\n' "$path_value" | tr ':' '\n'
}

# Remove duplicates in path
cleanpath() {
    export OLD_PATH="$PATH"
    local cleaned_path

    cleaned_path=$(printf '%s\n' "$OLD_PATH" | tr ':' '\n' | awk 'NF && !seen[$0]++' | tr '\n' ':' | sed 's/:$//')

    if [[ "$1" == "-d" ]]; then
        echo "Cleaned PATH without updating:"
        printf '%s\n' "$cleaned_path" | tr ':' '\n'
    else
        export PATH="$cleaned_path"
        if [[ "$1" != "-q" ]]; then
            echo "Updated PATH:"
            pathd
        fi
    fi
}

# Undo cleanpath
revertpath() {
    if [[ -n "$OLD_PATH" ]]; then
        export PATH="$OLD_PATH"
        echo "Reverted to OLD_PATH:"
        pathd
    else
        echo "No old PATH found to revert to."
    fi
}

fs() {
    local arg

    if du -b /dev/null >/dev/null 2>&1; then
        arg=-sbh
    else
        arg=-sh
    fi

    if (( $# > 0 )); then
        du "$arg" -- "$@" | sort -hr
    else
        du "$arg" .[^.]* ./* 2>/dev/null | sort -hr
    fi
}

tre() {
    tree -aC -I '.git|node_modules|bower_components' --dirsfirst "$@" | less -FRNX
}
